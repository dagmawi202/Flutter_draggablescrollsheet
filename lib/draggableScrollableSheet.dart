import 'package:flutter/material.dart';
    
class DraggableScrollableSheet extends StatelessWidget {

  const DraggableScrollableSheet({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}